# Roadmap

Tracking ongoing work, planned features, and ideas for The Companion.

### How to use this document

- **In Progress** — Move items here when you start working on them. Move them out when done.
- **Planned** — Concrete improvements ready to be picked up.
- **To Verify** — Completed features that need manual verification from the user. Each entry should include specific instructions on what to test and why automated testing wasn't sufficient. The user removes entries after verifying.
- **Ideas** — Rough ideas for future consideration.

When a feature is fully complete and doesn't need manual verification, just remove it from this document entirely — there is no "Completed" section. If there are known cases you couldn't test, add the feature to "To Verify" with clear verification instructions instead.

## In Progress

_Nothing currently in progress._

## Planned

- [ ] **Session branching (fork at earlier conversation point)** — Investigate adding the ability to branch/fork a session at an earlier point in the conversation, creating a new session that starts from that point. Both the Claude Code CLI and the VS Code extension already support this. This is a foundational capability that other features (like plan-approval-with-compaction) could build on top of.
- [ ] **Grouped terminal tool calls don't show individual output** — When consecutive Bash/terminal tool calls are grouped into a single "Terminal N" block, only the command descriptions are listed. There's no way to expand each command to see its output. Each command in the group should be expandable to reveal its stdout/stderr.

## To Verify

- [ ] **Codex skill parity: quest + backend-aware skills API** — Quest integration now writes `quest` skill into both `~/.claude/skills/quest` and `~/.codex/skills/quest` so new Codex session homes inherit it via existing seeding. `/api/skills*` now accepts `?backend=claude|codex|both` (default `both`) for list/get/create/update/delete. Verify: (1) start a new Codex session and confirm `quest` appears in available skills; (2) call `/api/skills?backend=claude`, `...codex`, and default to confirm scoping/union behavior; (3) create/update/delete with `backend=codex` and confirm only Codex skill files change.
- [ ] **Diff view: total additions/deletions** — The diff panel sidebar now shows per-file `+X -Y` stats next to each filename, a total `+X -Y` summary under the "Changed (N)" header, and per-file stats in the diff top bar next to the filename. Verify: make changes in a session (Edit/Write tools), switch to the Diff tab, and confirm the green/red stats appear in the sidebar and top bar. Check that totals update as new files are changed.
- [ ] **Plan mode: proper markdown rendering + minimize** — Plan mode now renders markdown with proper headings, lists, code blocks, tables, and blockquotes (previously rendered as unstyled HTML). Also added a minimize button (dash icon) to collapse the plan banner into a compact chip, matching the AskUserQuestion minimize behavior. Verify: trigger plan mode, confirm headings/lists/code render correctly; click the minimize button, confirm the chip shows a plan preview; click the chip to re-expand.
- [ ] **Worktree deletion confirmation on archive restored** — Upstream's container migration (`92a6172`) replaced the `isWorktree` check with `isContainerized` in the archive confirmation flow, so worktree sessions were archived without warning and without force-deleting the worktree. Restored the confirmation dialog for worktree sessions (shows "delete the worktree" vs "remove the container" depending on session type) and moved it inline next to the session item instead of at the top of the sidebar. Verify: archive a worktree session → confirmation should appear inline below that session with worktree-specific warning text. Clicking "Archive" should delete the worktree.
- [ ] **Fix permission prompts showing with Ask toggle OFF** — The post-plan mode transition was inverted: after ExitPlanMode approval, `askPermission=false` incorrectly resolved to `"acceptEdits"` instead of `"bypassPermissions"` (and vice versa). Fixed in both `ws-bridge.ts` and the `resolvePostPlanMode` helper. Verify: create a new session with Ask toggle OFF in both Plan and Agent modes. In Plan mode, approve the plan and confirm subsequent tool calls execute without permission prompts. In Agent mode, confirm tool calls never show permission prompts. Also verify that ExitPlanMode still shows the plan approval UI regardless of Ask toggle state.
- [ ] **Diff view: merge-base comparison + selectable base branch** — The diff panel previously compared against the tip of `main`, showing all changes between the current branch and main's tip. Now uses `git merge-base` to find the fork point, so only changes introduced on the current branch are shown. Also added a base branch selector dropdown in the diff panel header (persisted per-cwd to localStorage). This should also fix the "modified files as pure additions" bug — files that existed at the fork point but were modified on the branch should now show proper hunked diffs instead of `@@ -0,0 +1,N @@`. Verify: (1) open a session on a branch diverged from `main` — diff should only show that branch's changes, not all accumulated changes since main. (2) Modified files should show proper before/after hunks, not full-file additions. (3) Select a different base branch from the dropdown — diff should update. (4) Reload the page — the selected base branch should be restored.

## Ideas

- [ ] **Questmaster — native task management system** — A task tracking system built into Companion that Claude sessions can read and write via tool calls. Tasks flow through statuses: **Idea → Refined → In Progress → Needs Verification → Done**. Key design points:
  - Every session has access to Questmaster (no special modes). Brainstorming is just "start a session and talk about tasks."
  - One task = one agent. Break big tasks into subtasks rather than sharing across sessions.
  - Tasks link to the session that claimed them (`sessionId`), enabling click-to-navigate from the task UI to the working session.
  - When an agent finishes, it attaches **verification items** — only the checks it couldn't do itself (e.g. "confirm button renders on mobile"). User checks these off to move to Done.
  - **Priority over Haiku auto-tasks**: When a Questmaster task is claimed during a session, it overrides the Haiku-generated task labels for that duration. Haiku labels are the fallback for gaps where no Questmaster task is active. Only the main agent (Opus/Sonnet) makes Questmaster decisions — Haiku just names things for the sidebar.
  - Storage: server-side, human-readable JSON in `~/.companion/tasks/` — portable enough to `cat`, `jq`, or migrate elsewhere.
  - Agent tool calls: `task_list` (query/filter), `task_create` (new idea), `task_update` (edit description/status), `task_claim` (set In Progress + link session), `task_complete` (move to Needs Verification + attach checklist).

- [ ] **Plan approval with partial compaction** — In plan mode, the model often does extensive exploration (many tool calls, back-and-forth corrections) before arriving at the final plan. This consumes a lot of context tokens. Add a third option alongside "approve" and "reject": "approve & compact." This would accept the plan but partially compact the context by summarizing all the exploration and iteration that led to the plan, while keeping the earlier conversation history (before planning started) intact. The model would then see: (1) full pre-planning conversation, (2) a brief summary of the exploration/iteration process, (3) the final approved plan. This saves tokens, slows context growth, and allows more iteration. Implementation likely requires forking/rewriting the chat history at the point where planning began — session branching (see Planned) could be a building block for this.
- [ ] **Collapsible agent activity between user messages** — In long conversations, scrolling past many tool calls and agent messages to find previous user messages is tedious. Add a toggle to collapse all agent activity between two consecutive user messages into a single compact row. When collapsed, show a brief indicator (e.g. "12 agent actions"). Bonus: call Claude Haiku to generate a short summary of the collapsed agent activity so users can skim what happened without expanding.
- [ ] **Investigate Claude Code hooks compatibility** — User hooks (configured in `~/.claude/settings.json` or `.claude/settings.json`) may not fire correctly when sessions run inside Companion. Investigate whether Companion or the CLI launcher modifies/overrides hook configuration. If Companion does inject its own hooks, ensure they are composed with the user's existing hooks (e.g. chained or merged) rather than replacing them.
- [ ] **Persistent "always allow" permission rules** — When the user clicks "Allow always" on a permission prompt, Claude Code writes the rule to a local `.claude/settings.local.json` in the project. In a git worktree workflow, that file is lost when the worktree is deleted. Explore ways to make these permission rules more durable — e.g. Companion could intercept the "allow always" action and store rules server-side, or sync them to the user's global `~/.claude/settings.json`, or maintain its own permission allowlist that gets injected into new worktrees. Needs brainstorming on the right approach.
- [ ] **Server-side auto-approve as fallback for askPermission=false** — If the CLI permission mode fix proves insufficient (e.g. due to timing windows where the CLI sends permission requests before processing a mode change), add server-side auto-approval in `handleControlRequest`: when `session.state.askPermission === false` and the tool is not `ExitPlanMode`, automatically send an "allow" `control_response` back to the CLI without forwarding to the browser. This was implemented and then reverted because it introduces a second source of truth for permission decisions and risks silently approving tools if `askPermission` state gets out of sync. Only re-add if the CLI-mode-only approach has gaps.
- [ ] **Claude-Mem integration** — Connect the Claude-Mem observation database to the Companion UI. Claude-Mem runs an async worker that extracts learnings/summaries from each tool call. Once those observations are available, attach them to the corresponding tool call chips in the message stream. Users could expand a tool call to see what learnings were extracted from it (e.g. discoveries, decisions, bug findings). Requires querying the Claude-Mem API/DB and matching observations back to tool calls by session/timestamp.
